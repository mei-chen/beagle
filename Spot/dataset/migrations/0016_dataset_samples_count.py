# -*- coding: utf-8 -*-
# Generated by Django 1.10.5 on 2017-11-03 18:12
from __future__ import unicode_literals

from django.db import migrations, models


def update_samples_count_of_legacy_datasets(apps, schema_editor):
    """
    Replaces null (i.e. None) value from the samples_count field
    by the actual number of samples for each legacy dataset.
    """

    try:
        Dataset = apps.get_model('dataset', 'Dataset')
        for dataset in Dataset.objects.all():
            dataset.samples_count = len((dataset.samples or {}).get('texts', []))
            dataset.save()
    except LookupError:
        pass


class Migration(migrations.Migration):

    dependencies = [
        ('dataset', '0015_labelingtask_stages'),
    ]

    operations = [
        migrations.AddField(
            model_name='dataset',
            name='samples_count',
            field=models.IntegerField(blank=True, null=True, verbose_name='Samples count'),
        ),

        migrations.RunPython(
            update_samples_count_of_legacy_datasets,
            migrations.RunPython.noop,  # no reverse code needed
        ),
    ]
