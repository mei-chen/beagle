# -*- coding: utf-8 -*-
# Generated by Django 1.10.5 on 2017-04-27 13:22
from __future__ import unicode_literals

from django.db import migrations, models
import django.db.models.deletion


def delete_experiments_and_formulas(apps, schema_editor):
    """
    Deletes all experiments and formulas before adding the one-to-one
    'formula' field in order to prevent IntegrityErrors like
    'column "formula_id" contains null values' etc.
    """

    try:
        Experiment = apps.get_model('experiment', 'Experiment')
        Experiment.objects.all().delete()
    except LookupError:
        pass

    try:
        Formula = apps.get_model('experiment', 'Formula')
        Formula.objects.all().delete()
    except LookupError:
        pass


class Migration(migrations.Migration):

    dependencies = [
        ('experiment', '0001_initial'),
    ]

    operations = [
        migrations.RunPython(
            delete_experiments_and_formulas,
            migrations.RunPython.noop,  # no reverse code needed
        ),
        migrations.AddField(
            model_name='experiment',
            name='formula',
            field=models.OneToOneField(on_delete=django.db.models.deletion.CASCADE, to='experiment.Formula'),
        ),
    ]
